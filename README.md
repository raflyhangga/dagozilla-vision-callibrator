# Dagozilla's Vision Callibrator

## How to 

Prerequisites: Make sure that you've already run **communication-server** and **ROS from robot** first.

1. Run ```run.sh``` with following command
```
./run.sh [robotip]
```
2. Open ```http://localhost:3000```